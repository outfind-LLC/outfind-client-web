import { EmployerProfileFormLoader } from "@/features/profile/components/employer-profile-form-loader";

/** Company profile — the prototype company view (header, stats, about, contact,
 * details, job posts); editing swaps to the company form. */
export default function CompanyProfilePage() {
  return <EmployerProfileFormLoader />;
}
