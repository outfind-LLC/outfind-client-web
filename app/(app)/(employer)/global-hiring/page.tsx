import { GlobalHiringScreen } from "@/features/career/components/global-hiring-screen";

/**
 * Global hiring — employer only (the (employer) group layout guards the
 * audience). Pixel-perfect port of `_Peoplor_Design/career.js` (employer view).
 * The data each backend must supply is in `docs/api/career-and-global-hiring.md`.
 */
export default function GlobalHiringPage() {
  return <GlobalHiringScreen />;
}
