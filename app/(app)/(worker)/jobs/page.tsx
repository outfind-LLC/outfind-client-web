import { JobSearchLanding } from "@/features/jobs/components/job-search-landing";

/** Job Search tab — the worker's default surface: search modal + saved searches. */
export default function JobsPage() {
  return <JobSearchLanding />;
}
