import { CareerScreen } from "@/features/career/components/career-screen";

/**
 * Career & migration — worker only (the (worker) group layout guards the
 * audience). Pixel-perfect port of `_Peoplor_Design/career.js` (worker view). The
 * data each backend must supply is in `docs/api/career-and-global-hiring.md`.
 */
export default function CareerPage() {
  return <CareerScreen />;
}
