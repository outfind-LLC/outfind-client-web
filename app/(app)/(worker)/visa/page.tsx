import { VisaScreen } from "@/features/visa/components/visa-screen";

/**
 * Visa & work-permit guidance — worker only (the (worker) group layout guards
 * the audience). Placeholder for now; the detailed country-aware guidance
 * content will be added when provided.
 */
export default function VisaPage() {
  return <VisaScreen />;
}
