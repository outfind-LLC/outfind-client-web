import type { IconDef } from "./types";

/** Single check, stroke 2 (messenger sent tick / settings / employer). */
const check: IconDef = { inner: "<path d='M5 13l4 4L19 7'/>", sw: 2 };
export default check;
