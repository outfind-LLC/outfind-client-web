import type { IconDef } from "./types";

/** Left arrow, stroke 1.8 (messenger thread back). */
const back: IconDef = {
  inner: "<path d='M20 12H4'/><path d='M10 6l-6 6 6 6'/>",
  sw: 1.8,
};
export default back;
