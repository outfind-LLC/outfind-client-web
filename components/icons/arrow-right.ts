import type { IconDef } from "./types";

/** Right arrow with shaft (career "Plan route" / "View candidates" CTA). */
const arrowRight: IconDef = {
  inner: "<path d='M5 12h14M13 6l6 6-6 6'/>",
  sw: 2,
};
export default arrowRight;
