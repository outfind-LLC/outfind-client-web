import type { IconDef } from "./types";

/** Plus, stroke 1.8 (messenger attach / profile add). */
const plusBold: IconDef = { inner: "<path d='M12 5v14M5 12h14'/>", sw: 1.8 };
export default plusBold;
