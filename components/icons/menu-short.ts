import type { IconDef } from "./types";

/** Two-line asymmetric menu (messenger / profile mobile menu). */
const menuShort: IconDef = { inner: "<path d='M4 9h16M4 15h10'/>", sw: 1.7 };
export default menuShort;
